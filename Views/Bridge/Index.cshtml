<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Chọn Gói Dịch Vụ</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-gray-100 min-h-screen items-center justify-center">

    <div class="bg-white shadow-lg rounded-2xl p-8 max-w-lg w-full" style="margin-left: 35%;">
        <h1 class="text-2xl font-bold text-gray-800 mb-4">Chọn Hình Thức Tư Vấn</h1>

        <form method="get" class="space-y-4">
            <div>
                <label class="block text-gray-700 font-medium">Hình thức tư vấn</label>
                <select name="method" class="w-full border-gray-300 rounded p-2">
                    <option value="Online">Tư vấn Online</option>
                    <option value="Direct">Tư vấn Trực tiếp</option>
                </select>
            </div>

            <div>
                <label class="block text-gray-700 font-medium">Cấp độ dịch vụ</label>
                <select name="level" class="w-full border-gray-300 rounded p-2">
                    <option value="Basic">Cơ bản</option>
                    <option value="Premium">Nâng cao</option>
                </select>
            </div>

            <button type="submit" class="bg-blue-500 text-white px-4 py-2 rounded hover:bg-blue-600 transition">
                Xem chi tiết
            </button>
        </form>

        @if (Model != null)
        {
            <div class="mt-6 p-4 bg-gray-50 border border-gray-300 rounded">
                <h2 class="text-xl font-semibold text-gray-700">Kết Quả</h2>
                <p class="mt-2 text-gray-600">@Model</p>
            </div>
        }
    </div>

</body>

</html>
