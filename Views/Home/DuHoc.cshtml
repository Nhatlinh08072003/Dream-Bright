@{
    ViewData["Title"] = "Du học";
}

<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card shadow-lg">
                <div class="card-header bg-primary text-white text-center py-4">
                    <h2 class="mb-0">Chọn Điểm Đến Du Học</h2>
                </div>
                <div class="card-body p-5">
                    <form asp-controller="Consulting" asp-action="GetServices" method="get">
                        <div class="form-group">
                            <select name="country" class="form-control form-control-lg mb-4" required>
                                <option value="">-- Chọn quốc gia --</option>
                                <option value="US">Du học Mỹ 🇺🇸</option>
                                <option value="UK">Du học Anh 🇬🇧</option>
                            </select>
                        </div>
                        <button type="submit" class="btn btn-primary btn-lg btn-block w-100">
                            <i class="fas fa-search me-2"></i>Xem Thông Tin Du Học
                        </button>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>
